package view;

public interface baseUI {
    //show message through user interface
    void showMessage(String message);

    //show bunch of messages through user interface
    void showMessages(String[] messages);
}
